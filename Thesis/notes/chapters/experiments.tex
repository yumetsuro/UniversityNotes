% !TEX root = ../thesis.tex

% soft colors (print-friendly)
\definecolor{baseline}{RGB}{255,230,200} % light orange
\definecolor{newmodel}{RGB}{220,235,255} % light blue
% define colorbox for the baseline e new model

% define shortcutS for DeepCycleESN, DeepAntiESN, DeepCycleRON, DeepAntiRON
%% Model's Names
\newcommand{\DCR}{DeepCycleESN}
\newcommand{\DCRalt}{$\mathrm{DeepESN_{Cycle}$}}
\newcommand{\DAR}{DeepAntisymESN}
\newcommand{\DARalt}{$\mat/\hrm{DeepESN_{Antisym}$}}
\newcommand{\DCO}{DeepCycleRON}
\newcommand{\DCOalt}{$\mathrm{DeepRON_{Cycle}$}}
\newcommand{\DAO}{DeepAntisymRON}
\newcommand{\DAOalt}{$\mathrm{DeepRON_{Antisym}$}}

\label{ch:experiments}
\section{Introduction}
This chapter describes the hyperparameter optimization procedure employed to fairly compare different reservoir computing architectures across multiple benchmark tasks. 
In our empirical evaluation process aimed to assess the performance of the architectures proposed in Chapter \ref{ch:methodology}.
We consider a memory based tasks such as: short-term memory capacity (MC), long temporal dependacies with sequential classification benchmarks (FordA, FordB, sMNIST, psMNIST, and npCIFAR-10),
 and a temporal regression (Mackey--Glass).

\section{Memory Capacity}
\label{sec:exp:mc}
Memory is a fundamental aspect of recurrent neural networks, the capability of retaining and utilizing past information for currently processing inputs
is crucial for tasks involving temporal dependencies.
The Memory Capacity (MC) task was introduced by Jeager in \cite{memorycapacity} to quantify the capacity of recurrent networks to represent past inputs by correlating the past events of an i.i.d. input
stream with the network output. We assess short-term memory as follows:

Let $u(t)\sim \mathcal{U}[-0.8,0.8]$ be an i.i.d. input stream driving the system of discrete time steps $T=6000$ and let $x(t)\in\mathbb{R}^N$ be the state at time $t$.
For each delay $k\in\{1,\dots,K\}$ we train a linear readout $\hat{u}(t-k) = w_k^\top x(t)$ 
to reconstruct the input $u(t-k)$ from the current state $x(t)$.
 We define the memory capacity at delay $k$ as the squared correlation coefficient between the target $u(t-k)$ and the reconstructed signal $\hat{u}(t-k)$:

\begin{equation}
\mathrm{MC}_k \;=\; \frac{\mathrm{cov}^2\!\left(u(t-k),\hat{u}(t-k)\right)}{\mathrm{var}\!\left(u(t-k)\right)\,\mathrm{var}\!\left(\hat{u}(t-k)\right)} \in [0,1],
\end{equation}
and the total memory capacity as
\begin{equation}
\mathrm{MC} \;=\; \sum_{k=1}^{\infty} \mathrm{MC}_k.
\label{eq:mc_k}
\end{equation}

\paragraph{Experimental Setting}
In order to compute and compare the MC of our models with others, we adopted the same setting widely used in the RC community, using a reservoir size of $N_{r}=100$ units linearly activated and trained by Ridge Regression, 
setting the total lag $k=200$ (twice reservoir size) from equation \ref{eq:mc_k}, the reason behind this choice is that the maximum
MC achievable by an N-unit linear reservoir is bounded by $N$, a proof is provided by \cite[Proposition 2.]{memorycapacity}.

For the warmup period, we discarded the first $T_{\mathrm{wash}}=100$ timesteps to remove dependencies on initial conditions.  
The readout weights are trained on $5000$ timesteps, and then we evaluated the test MC on the last $900$ timesteps.
Since we are interested in exactly recalling the target signal, we set the regularization parameter $\lambda=0$ in the Ridge Regression.

For the remaining hyperparameters, we performed a random search to estimate the most important parameters affecting the MC performance, often
being the spectral radius and input scaling. We left the leakage rate $\alpha = 1.0$, $\omega_{in}$ and $\omega_{inter}$ both fixed at $0.1$ and for oscillators $0.01$

Our experiment was repeated over different numbers of layers by keeping the same number of total reservoir neurons, so 1 layer with $N_{r}=100$ units, 2 layers with $N_{r}=50$ units each, 4 layers with $N_{r}=25$, and so on.


%\paragraph{Reported results}
%We report $\mathrm{MC}$ and optionally the curve $\mathrm{MC}_k$ vs.\ $k$ to characterize how memory decays with delay.

\subsection{Results}
In order to assess whether adding modularity in deep reservoir architecture can improve performance in terms of memory capacity, we compare our results between the best configuration for each architecture.

%add table here % with ARCHITECTURE, MEMORY CAPACITY TRAIN/TEST
\begin{table}[htbp!]
\centering
\caption{Memory Capacity results for different architectures averaged over 3 trials from the best performing model. The higher the better.}
\label{tab:mc_results}
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Architecture} & \textbf{Train MC} & \textbf{Test MC} \\
\midrule
DeepESN & 60.3 $\pm$ 2.64 & 56.01 $\pm$ 2.45  \\
\DCR & 99.1 $\pm$ 0.1 & \underline{\textbf{99}} $\pm$ 0.1 \\
\DAR & 60.51 $\pm$ 2.16 & 57.83 $\pm$ 2.8  \\  
% add line here for different architectures
\midrule 
DeepRON & $50.58 \pm 2.2$  & $47.55 \pm 3.1$   \\    
\DCO & 41.9 $\pm$ 2.13 & 38.86 $\pm$ 2.52 \\
\DAO & 53.35 $\pm$ 2.16 & \underline{\textbf{52.10}} $\pm$ 1.8 \\
\bottomrule
\end{tabular}

\end{table}

The results in Table \ref{tab:mc_results} show that with the proposed \DCR\@ we are able to achieve the same performance as the SCR. This is true because in the case of linear \DCR\@ with 1 unit per layer, we get back exatly SCR model.
The \DAR\@, instead, doesn't improve in terms of memory MC, with respect to the DeepESN. 
%We observed a behaviour in the first steps of certain architecture variants where %at the first delays the network struggle to reconstruct the signal, because of %the past states brought by antisymmetric contributions, if the $\epsilon$ is too %high we can risk to add interference. 

For the oscillator-based reservoirs, we can't recover the same behaviour in case of \textit{cycle} architecture as for ESNs; this is probably due to the fact that the oscillators' dynamics need to be carefully tuned layerwise to achieve higher performances. But for \DAR\@ the antisymmetric coupling helps with the
In particular, in DeepRONs, the eigenvalues' location are decided by tuning the $\gamma$ and $\epsilon$ parameters. Such a system is very sensitive to those hyperparameters in this task, so further investigation is needed to decide whether a heterogeneous setting of deep oscillator reservoirs retains more information.

In the figure \ref{fig:mc_\DCR_layers} below we can see the MC curves for \DCR\@ as we increase the number of layers, in this example we fixed $\rho=0.5$ to ensure contractivity, for what we said in \ref{sec:method:cycle_architecture} a too high 
spectral radius can lead to a global system which is unstable ($\rho > 1$).
The 100 units in cyclically connected perfectly recall the input up to a delay $99$, as soon as we halve the number of layers, having 2 units in each layer (yellow line in the figure), the performances drop but still remains high, yielding a test of $MC = 67.3$.

\begin{figure}[htbp!]
    \centering
    \includegraphics[width=0.9\textwidth]{images/memory_capacity_vs_delay_esn.png}
    \caption{Memory Capacity curves for \DCR\@ with different number of layers.}
    \label{fig:mc_\DCR_layers}
\end{figure}

\subsubsection{Memory Capacity in Edge of Chaos regime}

Finally, considering the case where we move in \textit{edge of chaos} regime, the \DCR\@ architecture, by setting a spectral radius of about $\rho = 0.7$, we get a $\rho_\text{global} \approx 0.99$, and the memory capacity increases up to $MC = 74.3$ for the 2 units per layer modularity. In the figures below we can see both the MC curve and a plot of the Jacobian eigenvalues distribution.
% insert figure with the curve up and below the eigenvalues give a lot of space to both the images
\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{images/memory_capacity_vs_delay_DCR_Edgeofchaos.png}
    \caption{Memory Capacity curve for \DCR\@ in edge of chaos regime.}
    \label{fig:mc_\DCR_eoc}
\end{figure}
% eigenvalues

(and reducing the number of units per layer accordingly), the eigenvalues of the Jacobian tend to spread more and more around the unit circle,
 this is due to the fact that each layer has a spectral radius less than 1, but when we compose multiple layers, the global system tends to have eigenvalues with modulus very close to 1.
\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.8\textwidth]{images/eigenvalue_distributions_esn_edgeofchaos.png}
    \caption{Jacobian eigenvalues distribution for \DCR\@ in edge of chaos regime.}
    \label{fig:mc_\DCR_eoc_eigenvalues}
\end{figure}

\section{Long temporal dependencies tasks}  
This set of experiments aims to evaluate the ability of 
different reservoirs to capture in different ways long temporal dependancies,
 we consider three different sequential image classification 
tasks on two dataset MNIST and CIFAR-10.

\subsection{Sequential MNIST (sMNIST)}
The Sequential MNIST task treats each $28 \times 28$ grayscale image as a sequence of 784 scalar values, by flattening the image matrix,
 thus destroying spatial information in favor of temporal structure. The pixels are presented in a fixed order (row-wise). Data is presented one pixel at a time, so the model must classify the digit (0--9) after observing the entire sequence. 
 This task evaluates a model's ability to retain information over long temporal horizons while processing one-dimensional sequential inputs.
% add example of smnist image as sequence

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.7\textwidth]{images/smnist.png}
    \caption{Example of Sequential MNIST (sMNIST) input sequence. Each pixel of the $28 \times 28$ image is presented sequentially as a scalar input over 784 timesteps.}
    \label{fig:smnist_sequence}
\end{figure}

\subsection{Permuted Sequential MNIST (psMNIST)}
The Permuted Sequential MNIST task applies a fixed random permutation to the pixel ordering of sMNIST sequences. This destroys the spatial structure of the original images, making the task purely sequential and significantly more challenging. The permutation is generated deterministically using a fixed random seed to ensure reproducibility.

\subsection{Noise-Padded CIFAR-10 (npCIFAR-10)}
The Noise-Padded CIFAR-10 task consists to take CIFAR-10 dataset that contains $32 \times 32 \times 3$ color images across 10 classes. 
Whe first two dimensions representing spatial dimensions and the third dimension representing RGB color channels.
Each image is flattened row-wise into a sequence representation of 32 rows, where each row contains 96 values (32 pixels $\times$ 3 channels).
We fed 96-dimensional vectors sequentially over 32 timesteps, then for the remaining 968 timesteps we add uniform random noise in the range $[-1, 1]$, resulting in sequences of length 1000. 
This setup tests the model's ability to extract and maintain relevant information while being exposed to subsequent irrelevant noise.

\section{Experimental Setting}
\subsection{Parameter matching}
To ensure fair comparison across architectures,
 we employed a parameter matching strategy that adjusts the number of hidden units 
 per layer to achieve approximately 100,000 reservoir parameters. 
For the readout we keep all the layer states concatenated, resulting in readout dimension equal the the number of units per layer multiplied by the number of layers. 
The number of units depends on:
\begin{itemize}
    \item The number of layers
    \item The input dimensionality (1 for univariate tasks, 96 for npCIFAR-10)
    \item The architecture type (ie. \DCR\@ with no recurrence blocks have fewer parameters due to zero recurrence, so we increase unit in cycle blocks)
\end{itemize}

\subsection{Results}
In the table below, we report results for the three sequential image classification tasks for each architecture across the datasets.
We use a the entire dataset consisting of 60,000 
\begin{table}[ht!]
    \centering
    \caption{Performance Summary. Comparison of the best test accuracy over 3 trials achieved by each architecture across the three datasets.
    We higlight in \ \colorbox{newmodel}{\strut proposed modular models}\ and\ \colorbox{baseline}{\strut shallow and deep models}.
    With $\pm$ we indicate the standard deviation across trials.}
    \label{tab:performance_summary}
    \begin{tabular}{lccc}
        \toprule
        \textbf{Architecture} & \textbf{sMNIST $\uparrow$} & \textbf{psMNIST $\uparrow$} & \textbf{npCIFAR-10 $\uparrow$} \\
        \midrule
        \rowcolor{baseline}
        ESN                 &$82.93\% \pm 0.89$& $78.87\% \pm 1.82$ & $32.06\% \pm 0.19$ \\
        \rowcolor{baseline}
        DeepESN             &$85.94\% \pm 0.46$& $ 80.62\% \pm 0.95$ & $\underline{\mathbf{39.83\%}} \pm 0.19$ \\
        \rowcolor{newmodel}
        \DCR     & $86.81\% \pm 0.49$ & $75.92\% \pm 0.90$ & $32.28\% \pm 0.19$ \\
        \rowcolor{newmodel}
        \DCR\@ (No Recurrence) & $86.32\% \pm 0.35$  & $79.31\% \pm 1.62$ & $33.91\%  \pm 0.39$ \\
        \rowcolor{newmodel}
        \DAR & $\underline{\textbf{88.29\%}} \pm 0.89$ & $\underline{\textbf{81.33\%}} \pm 0.65$ & $34.37\% \pm 0.16$ \\
        \midrule 
        \midrule
        \rowcolor{baseline}
        RON                 & $\underline{\textbf{90.7\%}} \pm 0.51$ & $\underline{\textbf{86.5\%}} \pm 1.23$ & $28.42\% \pm 0.21$ \\
        \rowcolor{baseline}
        DeepRON             & $84.8\% \pm 1.4$ & $81.6\% \pm 1.3$ & $26.82\% \pm 0.1$ \\
        \rowcolor{newmodel}
        \DCO     & $89.17\% \pm 0.8$ & $51.03\% \pm 0.8$ & $26.36\% \pm 0.23$ \\
        \rowcolor{newmodel}
        \DCO\@ (No Recurrence) & $61.25\% \pm 0.51$ & $39.6\% \pm 1.4$ & $26.87\% \pm 0.12$ \\
        \rowcolor{newmodel}
        \DAO & $88.7\% \pm 0.62$ & $83.32\% \pm 2.1$ & $\underline{\textbf{32.12\%}} \pm 0.15$ \\
        \bottomrule
    \end{tabular}
\end{table}

DeepESNs achieve substantially higher accuracy than the other architectures on the npCIFAR-10 task. 
This behavior can be attributed to their hierarchical structure, where,
successive layers operate as filters, progressively transforming the input representation.
The selective filter bank is attenuating irrelevant components introduced by noise padding while preserving informative
features contained in the initial portion of the sequence.

In contrast, modular architectures such as \DCR\@ and \DAO\@ show weaker performances on npCIFAR-10. 
In these models, information is continuously recirculated between the last and first layers, or, in the case of \DAR\@, between near modules.
Preventing a clear hierarchical separation of representations. This global mixing may facilitate the propagation 
of noise introduced during the padding phase, making it more difficult for the system to isolate and preserve the
 informative portion of the sequence.

On the other hand, the behavior observed on sMNIST and psMNIST is markedly different. 
In these tasks, antisymmetric coupling consistently improves performance.
 Notably, \DCR\@ both with and without intra-layer recurrence performs better than DeepESN.
  This suggests that cyclic transport mechanisms alone may be sufficient to preserve long-term information in purely sequential tasks,
   possibly by extending the effective memory horizon of the system.

This do not translates to oscillator architectures, in fact, 
in the latter, oscillatory dynamics appear to rely strongly on intra-layer recurrent connections.
Removing recurrence in these systems leads to a significant drop in performance, particularly on psMNIST, 
indicating that, unlike ESN architectures where structured transport can compensate for reduced recurrence,
 oscillator reservoirs fundamentally depend on their internal recurrent structure to sustain meaningful temporal dynamics.

\section{Time Series Classification and Regression Tasks}

\subsection{FordA and FordB}
The Ford datasets are binary classification tasks from the UCR Time Series Archive. 
FordA contains 3601 training and 1320 test samples, while FordB contains 3636 training and 810 test samples. 
Each sample is a univariate time series of length 500, representing sensor measurements from automotive engine 
systems. The task is to classify whether a specific symptom is present.
The Ford datasets are challenging due to their long temporal dependencies and subtle class differences.
% PUT Forda and Fordb in subplot
\begin{figure}[ht!]
    \centering
    \begin{subfigure}[b]{0.5\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/forda.png}
        \caption{Example from FordA dataset}
        \label{fig:fordA_example}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.5\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/fordb.png}
        \caption{Example from FordB dataset.}
        \label{fig:fordB_example}
    \end{subfigure}
    \caption{Examples of time series from FordA and FordB datasets. Each series has a length of 500, and the task is to classify the presence of a specific symptom.}
    \label{fig:ford_datasets}
\end{figure}

\subsection{Mackey-Glass Time Series Prediction}
The Mackey-Glass (MG) system is a classical chaotic time series benchmark defined by the delay differential equation:
\begin{equation}
\frac{dx(t)}{dt} = \beta\frac{x(t-\tau)}{1+x(t-\tau)^n} - \gamma x(t),
\label{eq:mg}
\end{equation}
We use standard parameters that produce chaotic dynamics ($\tau = 17$, $n = 10$, $\beta = 0.2$, $\gamma = 0.1$). The task is 84-step ahead prediction, i.e., given $x(t)$, predict $x(t+84)$.
We employ a washout period of 200 timesteps to allow the reservoir state to stabilize. Performance is measured using the Normalized Root Mean Squared Error (NRMSE):
\begin{equation}
\text{NRMSE} = \frac{\sqrt{\frac{1}{N}\sum_{i=1}^{N}(\hat{y}_i - y_i)^2}}{\sigma_y}
\end{equation}
where $\sigma_y$ is the standard deviation of the target signal.
Predicting MG time series requires modeling long‑term dependencies and 
chaotic dynamics. Because the system’s evolution depends on delayed past values,
 models must remember inputs many steps back, we use this tasks for assessing the stability of our architectures in long-term prediction.   

 % image of mg exampel
\begin{comment}
 \begin{figure}[ht!]
    \centering
    \includegraphics[width=0.8\textwidth]{images/mg84.png}
    \caption{Example of Mackey-Glass time series. The task is to predict the value 84 steps ahead based on past observations.}
    \label{fig:mackey_glass_example}
\end{figure}
\end{comment}

\begin{comment}
% create a subfigure with mg84 put in the same sub figure also  images/mg17.png (3d plot of the attractor)
\begin{figure}[ht!]
    %now subfigures
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/mackey_glass_3d.png}
        \caption{Mackey-Glass attractor for $\tau=17$.}
        \label{fig:mg17_attractor}
    \end{subfigure}
    \hfill
    % move up the second subfigure a bit
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/mg84.png}
        \caption{Mackey-Glass time series for $\tau=17$.}
        \label{fig:mg84_time_series}
    \end{subfigure}
\end{figure}
\end{comment}
% change the plot such that the mackeyglass 3d is above and below across the x axis of the plot you put the time series a little bit smaller
\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.6\textwidth]{images/mackey_glass_3d.png}
    \includegraphics[width=0.6\textwidth]{images/mg84.png}
    \caption{Mackey-Glass attractor (top) and time series (bottom) for $\tau=17$.}
    \label{fig:mg84_attractor_timeseries}
\end{figure}


 In figure \ref{fig:mg84_attractor} we have a plot of the 3D Mackey-Glass attractor 
consisting the state evolution over time, the three axis represent $x(t)$, $x(t-\tau)$ 
and $x(t-2\tau)$. While in \ref{fig:mg84_time_series} we have the 1D time series of MG showing how the signal is reconstructed after 84 steps.
Predicting this a very high lag as we do is a difficult task, that requires the model to retreive information from long past memory, this is also harder for
MG system with chaotic dynamics, our chosen $\tau=17$ is a standard setting from ESN literature. 

\subsection{Results}
In table \ref{tab:ford_results} below we report classification accuracy for FordA and FordB datasets.

\begin{table}[ht!]
    \centering
    \caption{FordA and FordB classification accuracy (\%) for different architectures.}
    \label{tab:ford_results}
    \begin{tabular}{lcc}
        \toprule
        \textbf{Architecture} & \textbf{FordA $\uparrow$} & \textbf{FordB $\uparrow$} \\
        \midrule
        \rowcolor{baseline}
        ESN                 & $ 67.5\% \pm 0.2$ & $58.2\% \pm 0.4$ \\
        \rowcolor{baseline}
        DeepESN             & $71.2\% \pm 0.16$ & $62.9\% \pm 0.3$ \\
        \rowcolor{newmodel}
        \DCR     & $69.8\% \pm 0.3$ & $63.3\% \pm 0.2$ \\
        \rowcolor{newmodel}
        \DCR\@ (No Recurrence) & $70.5\% \pm 0.9$ & $62.9\% \pm 0.2$ \\
        \rowcolor{newmodel}
        \DAR & $\underline{\mathbf{72.3 \pm 0.84}}$ & $\underline{\mathbf{63.7\% \pm 0.1}}$ \\
        \midrule
        \midrule
        \rowcolor{baseline}
        RON                 & $\underline{\mathbf{71\% \pm 0.2}}$ & $64\% \pm 0.7$ \\
        \rowcolor{baseline}
        DeepRON             & $66.3\% \pm 1.1 $ & $\underline{\mathbf{69\% \pm 1.3}}$ \\
        \rowcolor{newmodel}
        \DCO     & $65.2\% \pm 0.6$ & $67.1\% \pm 0.3$ \\
        \rowcolor{newmodel}
        \DCO\@ (No Recurrence) & $55.5\% \pm 0.03$ & $57.1\% \pm 0.01$ \\
        \rowcolor{newmodel}
        \DAO & $69\% \pm 0.02$ & $67.8\% \pm 0.8$ \\
        \bottomrule
    \end{tabular}
\end{table}

Within the ESN family, \DAR\@ achieves the best performance on both FordA and FordB. 
This confirms that antisymmetric coupling may improve stability and information propagation without
excessively mixing representations across modules. DeepESN also performs competitively, 
supporting the idea that hierarchical processing contributes positively to structured time-series classification. 
\DCR\@ without intra-layer recurrence remains competitive, further reinforcing 
the observation that pure cycle mechanisms alone can preserve sufficient temporal information for
these tasks. So introducing structured connectivity can compensate the reduction of recurrence blocks.

The oscillator architectures exhibit a different behavior. 
On FordA, the original RON achieves the highest accuracy within its family, while removing recurrence 
in \DCO leads to a substantial performance drop, confirming that oscillatory reservoirs
 rely on intra-layer recurrent dynamics to sustain meaningful temporal representations. 

On FordB, however, DeepRON achieves the best performance among all models, 
indicating that hierarchical stacking of oscillatory modules can provide additional representational
 capacity when the task benefits from multi-scale temporal processing.

Overall, these results highlight a structural distinction between the two reservoir families:
 while ESN architectures can maintain competitive performance through structured modularity,
  oscillator reservoirs remain strongly dependent on their intrinsic recurrent dynamics.

The table \ref{tab:mackeyglass_results} below reports Mackey-Glass 84-step ahead prediction NRMSE 
for different architectures.

\begin{table}[ht!]
    \centering
    \caption{MG84 prediction NRMSE for different architectures. Lower is better. }
    \label{tab:mackeyglass_results}
    \begin{tabular}{lc}
        \toprule
        \textbf{Architecture} & \textbf{MG84 NRMSE $\downarrow$} \\
        \midrule
        \rowcolor{baseline}
        ESN                 & $3\times 10^{-2} \pm 2.4\times 10^{-3}$ \\
        \rowcolor{baseline}
        DeepESN             & $\underline{\mathbf{1.2\times 10^{-2}}} \pm 4\times 10^{-3}$ \\ 
        \rowcolor{newmodel}
        \DCR     & $ 2.3\times 10^{-2} \pm 4\times 10^{-3}$ \\
        \rowcolor{newmodel}
        \DCR\@ (No Recurrence) & $ 5.1 \times 10^{-2} \pm 4\times 10^{-3}$ \\
        \rowcolor{newmodel}
        \DAR & $2 \times 10^{-2} \pm 1 \times 10^{-2}$ \\ %check 
        \midrule
        \midrule
        \rowcolor{baseline}
        RON                 & $\underline{\mathbf{1.8 \times 10^{-2}}} \pm 6\times 10^{-3}$\\
        \rowcolor{baseline}
        DeepRON             & $2.2 \times 10^{-2} \pm 5\times 10^{-3}$ \\ %% riguarda i risultati qui
        \rowcolor{newmodel}
        \DCO     & $4 \times 10^{-1} \pm 2\times 10^{-3}$ \\ % fa schifo
        \rowcolor{newmodel}
        \DCO\@ (No Recurrence) & $4.5 \times 10^{-1} \pm 3.6 \times 10^{-3}$ \\ 
        \rowcolor{newmodel}
        \DAO & ${1.8 \times 10^{-1} \pm 3.3 \times 10^{-2}}$ \\ %repeat this
        \bottomrule
    \end{tabular}
\end{table}

The Mackey–Glass (MG84) time series prediction task provides insights into the ability 
of different reservoir architectures to model chaotic dynamics.

DeepESN achieves the lowest prediction error, compared to shallow and other modular variants. 
This suggests that hierarchical recurrence enhances the reconstruction of complex temporal dependencies by
 progressively transforming and stabilizing internal representations. 
In contrast, \DCR\@ without intra-layer
recurrence exhibits a significant performance degradation, indicating that cyclic transport 
alone is insufficient for accurate chaotic prediction. This implies that \DCR\@ can rely more on it's memory
capabilities than the richness of reservoir dynamics.

The oscillator architectures display an even more pronounced dependency on recurrence. 
While RON and DeepRON achieve competitive errors, the \DCO variants suffer a dramatic increase in prediction error.
This could be attributed to the unstable behaviour we observed in Deep variants of oscillator reservoirs.

\todo{forse dovrei scrivere qualcos'altro...}

\clearpage
\section{Experimental details}

\subsection{Bayesian Optimization Framework}

For the rest of the experiments we employed Optuna \cite{optuna_2019} for Bayesian hyperparameter optimization.
 Optuna uses the Tree-structured Parzen Estimator (TPE) \cite{bergstra2011algorithms}
  as its default sampling strategy, which we utilized for the heavy hyperparameter search involved in our experiments, given the great number of parameters to tune for each architecture.

The TPE algorithm models the conditional probability $p(x|y)$ of hyperparameters $x$ given the objective value $y$ using two density estimators:
\begin{equation}
p(x|y) = \begin{cases}
\ell(x) & \text{if } y < y^* \\
g(x) & \text{if } y \geq y^*
\end{cases}
\end{equation}
where $y^*$ is a quantile threshold (typically the best 25\% of observations), $\ell(x)$ 
is the density formed by observations with objective values below $y^*$, and $g(x)$ is formed by the remaining observations.
 New hyperparameter configurations are sampled by maximizing the ratio $\ell(x)/g(x)$,
  which approximates the Expected Improvement acquisition function.

\subsection{Hyperparameter Search Spaces}

\subsubsection{Echo State Network Hyperparameters}

Table \ref{tab:esn_hyperparams} presents the hyperparameter search space for ESN-based models: DeepESN, \DCR\@ and \DAR.

\begin{table}[ht!]
\centering
\caption{All parameters with log scale are sampled from a log-uniform distribution\footnote{We use a log-uniform distribution to ensure uniform sampling in the logarithmic space, which allows for a more balanced exploration of hyperparameters across several orders of magnitude.}.}
\label{tab:esn_hyperparams}
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Hyperparameter} & \textbf{Range} & \textbf{Scale} & \textbf{Description} \\
\midrule
$\rho$ & $[0.1, 9.0]$ & Log &  Spectral radius \\
$\omega_{\text{in}}$ & $[0.1, 1.0]$ & Log &  Input scaling \\
$\omega_{\text{inter}}$ & $1.0$ & Fixed & Inter-layer scaling \\
$\alpha$ & $[0.001, 1.0]$ & Log & Leaky integrator coefficient \\
$\gamma$ & $[0.0001, 0.5]$ & Log & Diffusion regularizer \\
$\epsilon$ & $20.0$ & Fixed & Inter-layer coupling strength \\
Number of layers & $\{1\}$ or $\{5, 10\}$ & Categorical & Number of Modules \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Randomized Oscillators Network Hyperparameters}

Table \ref{tab:ron_hyperparams} presents the hyperparameter search space for RON-based models: DeepRON, \DCO and \DAO.

\todo{questa tabella forse posso togliere la descrizione e metterla anche nell'appendice...}
\begin{table}[ht!]
\centering
\caption{RON hyperparameter search space.}
\label{tab:ron_hyperparams}
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Hyperparameter} & \textbf{Range} & \textbf{Scale} & \textbf{Description} \\
\midrule
$\tau$ & $[10^{-4}, 1.0]$ & Log & Integration time step \\
$\rho$ & $[0.1, 9.0]$ & Log & Spectral Radius \\
$\omega_{\text{in}}$ & $[0.05, 2.0]$ & Log & Input scaling \\
$\omega_{\text{inter}}$ & Set to $\omega_{\text{in}}$ & Fixed & Inter-layer scaling \\
$\epsilon_c$ & $[0.01, 5.0]$ & Log & Inter-layer coupling \\
$\gamma_c$ & $[0.0001, 0.5]$ & Log & Diffusive strength \\
$\gamma$ (damping) & $1.0 \pm 0.05$ & Fixed &  Damping  \\
$\epsilon$ (stiffness) & $1.0 \pm 0.05$ & Fixed &  Stiffness \\
Number of layers & $\{5, 10\}$ & Categorical & Architecture-dependent \\
\bottomrule
\end{tabular}
\end{table}


For the sequential image classification tasks (sMNIST, psMNIST, npCIFAR-10), we performed \textbf{100 trials} per model configuration, optimizing for validation accuracy (maximization). For the UCR time series tasks (FordA, FordB, Mackey-Glass), we performed \textbf{100 trials} per configuration, optimizing for validation accuracy (maximization) for classification tasks and NRMSE (minimization) for Mackey-Glass.

All experiments used:
\begin{itemize}
    \item Random seed: 42 (for TPE sampler reproducibility)
    \item Validation split: 20\% holdout from training data
    \item Readout: Ridge regression (Mackey-Glass) or Logistic regression (classification tasks)
    \item Feature scaling: StandardScaler applied to reservoir activations
\end{itemize}

\section{Summary}\todo{se serve...}